# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.12   # required by FastAPI for UploadFile/Form parsing
pydantic[email]>=2.9,<3.0

# --- Database ---
motor>=3.6,<4.0
pymongo>=4.9,<5.0

# --- Auth ---
pyjwt>=2.9,<3.0
bcrypt>=4.2,<5.0
pyotp>=2.9,<3.0   # TOTP for admin MFA

# --- Config ---
python-dotenv>=1.0,<2.0

# --- Rate limiting ---
slowapi>=0.1.9,<0.2

# --- LLM (Gemini, direct) ---
google-genai>=0.7,<1.0

# --- Object storage (S3-compatible: AWS S3 / Cloudflare R2 / Backblaze B2 / MinIO) ---
boto3>=1.35,<2.0

# --- Payments ---
razorpay>=1.4,<2.0

# --- Web crawling / knowledge ingestion ---
aiohttp>=3.10,<4.0
beautifulsoup4>=4.12,<5.0
rank-bm25>=0.2.2,<0.3
pypdf>=5.0,<6.0
python-docx>=1.1,<2.0

# --- Scheduled jobs ---
apscheduler>=3.10,<4.0

# --- Admin system monitor ---
psutil>=6.0,<7.0
