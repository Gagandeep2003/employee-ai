# Copy to .env for local dev. In production this is set at BUILD time
# (see frontend/Dockerfile's REACT_APP_BACKEND_URL build arg), since Create React
# App bakes REACT_APP_* vars into the static bundle at build time, not runtime.
REACT_APP_BACKEND_URL=http://localhost:8000
